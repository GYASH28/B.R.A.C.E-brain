# BRACE production audit

Baseline commit: `4d676edb571c1e7432b95723ec2d17aaa522c3e9`  
Starting application version: `0.7.0`  
Audit scope: desktop renderer, Electron boundary, indexing, local data, graph, automations, public website, packaging, and release infrastructure.

This register records evidence, intended proof, and actual status. A checked implementation item is not a claim that BRACE is production/stable ready; the completion gates at the end remain authoritative.

| Severity | Subsystem | Evidence / risk | Intended fix and proof | Status |
| --- | --- | --- | --- | --- |
| P0 | Electron IPC | `electron/memory-service.ts` previously registered privileged handlers without validating their renderer origin. | Route every handler through one exact-origin/top-frame guard; regression-test packaged, development, malformed, and hostile origins. | Implemented; tests in `tests/ipc-trust.test.js`. |
| P0 | IPC payloads | Main-process handlers previously accepted unbounded `any` payloads. | Validate every registered channel at runtime, reject unknown fields, and cap strings/lists/objects. | Implemented contract gateway in `src/shared/ipc/schemas.ts` and typed preload surface; contract tests pass. |
| P0 | Project content | Ordinary source files could contain credentials even when filenames looked safe. | Best-effort content scanning/redaction before chunks or embeddings; never log recovered values. | Implemented with synthetic regression coverage; documented as defense in depth. |
| P0 | Framework dependencies | The original production prompt identified Next.js 16.3.2 as requiring review; the August 2026 security release requires 16.3.3 or newer. | Upgrade to the current Active LTS patch, refresh lockfile, audit, build, package, and run E2E. | Upgraded to 16.3.4 with matching ESLint config; dependency and license gates pass, as do the static build, Electron package, and packaged Linux smoke tests. |
| P1 | Graph scale | Snapshot exposed only 500 records per graph category and layout collision work was quadratic. | Support a 10k-class logical graph, render bounded LOD, and spatially index collision relaxation. | Implemented; 10k projection and maximum-detail layout benchmarks pass. |
| P1 | Graph fullscreen | Browser Fullscreen API behavior was unreliable inside Electron and could fail silently. | App-level focus surface with visible exit, `F`, and `Escape`; verify viewport bounds in browser and Electron. | Implemented and verified in the synthetic Electron journey, including focus entry/exit, viewport bounds, all five projections, zoom, and responsive mode. |
| P1 | Graph accessibility | SVG was the only complete traversal surface. | Add keyboard spatial traversal and a list-equivalent node index tied to current filters/detail. | Implemented and verified through keyboard traversal, named controls, equivalent node index, focus, and reduced-motion checks. |
| P1 | Project indexing thread | Traversal and file reads previously executed from the trusted Electron process. | Move traversal/read/chunk/scan to a Worker with a narrow protocol, cancellation, bounded progress, and staged source commits. | Implemented in `core/project-index-worker.js` / `core/project-index-jobs.js`; cancellation and atomic-source tests pass. Optional watching uses native recursive events where safe and bounded metadata-only project-tree polling on Windows, avoiding Node 24's process-aborting file-event defect. |
| P1 | Recovery | Backups existed without a first-class validated restore and pre-migration backup flow. | Add integrity preview, current-state safety backup, atomic restore, restart, and old-schema fixtures. | Implemented in `core/database-recovery.js` and Settings. Restore is integrity-checked, staged while the live database remains untouched, and atomically applied at the next startup after a safety backup; migration/recovery tests pass. |
| P1 | Distribution | Windows artifacts are unsigned and no verified updater path exists. | Add signing, timestamp verification, provenance, upgrade tests, then opt-in signed updates. | Artifact provenance is wired for tagged builds. Signing and an updater remain external release blockers; packages remain preview-only. |
| P1 | App architecture | `brace-app.tsx` and `memory-store.js` were large multi-domain modules. | Split by real domain seams with behavior and regression coverage. | In progress: Graph, Automations, Organization, task center, page primitives, desktop adapters, indexing/recovery/import/watch services, and organization repository are extracted. Further view/repository splits are maintainability work, not a data-safety blocker. |
| P1 | Enterprise product | No organization or workspace boundary existed. | Preserve Personal local memory and add optional governed workspaces with transparent ownership. | Local organization/workspace/member/role/audit foundation implemented. Networked collaboration, SSO/SCIM and remote policy enforcement are deliberately not claimed. |
| P1 | Business authorization | Role labels alone must never become an authorization boundary. | Add provider-scoped identities and a pure, deny-by-default decision kernel; enforce exact organization, workspace, membership, resource, personal-owner, and service grants inside each protected operation. | Schema v8 and the kernel are implemented without identity/owner backfill. Schema v9 adds strict Ed25519 assertion verification, replay-resistant local sessions, in-memory current-session state, expiry checks, and immediate local subject-status revocation. Electron workspace-member mutation and dedicated shared publication/read/search/graph/export/context APIs resolve identity from the private service context. There is no default trusted issuer or renderer enrollment route, so unprovisioned actions fail closed. Full OIDC/SAML/SCIM provisioning, multi-device revocation, remote sync/ingestion, and enforcement for organization creation, backup, connectors, automations, audit reads, and MCP remain incomplete; production authorization is not claimed. |
| P1 | Company synchronization | A generic database sync would mix personal and company authority, leak plaintext or keys, and silently lose offline conflicts. | Sync only explicit shared projections through tenant-scoped authenticated encryption, opaque key references, authorized devices, leases, idempotent operations, conflicts, tombstones, and revocation. | Schema v10 implements and tests the transport-neutral local operation log and replica core. Private source metadata is absent; ciphertext is authenticated to exact tenant/workspace/actor/device/record/revision metadata; keys are externally resolved and erased after use; reorder, replay, tamper, expiry, cross-boundary access, tombstones, device revocation, and workspace key-reference/replica removal are covered. No network transport, server policy, object storage, production keystore, or multi-tenant deployment is claimed. |
| P1 | Agent approval boundary | A future agent or connector could otherwise convert a preview into an unreviewed, replayable external effect. | Bind one inspectable, bounded plan hash to an expiring human decision and one-time service use; record minimized lifecycle evidence in a verifiable chain. | Schema v11 implements and tests service-only requests, manager/owner decisions, no self-approval, exact-plan mismatch invalidation, expiry, single-use consumption, revocation denial, a local tamper-evident audit chain, and trusted-IPC workspace evidence export that refuses a broken chain. It does not execute connectors or grant external access; remote audit checkpoints, service enrollment, execution-time budgets, cancellation, and compensation remain unshipped. |
| P2 | Company brief | Leaders and employees lacked a simple, privacy-scoped view of shared knowledge and attention. | Add role-oriented presentation lenses over approved shared records, source health, decisions, warnings, search, and direct detail while excluding personal/private data. | Implemented and verified with unit tests and synthetic Electron E2E. Employee, Manager, Executive, and Administrator lenses are presentation only and never grant access. |
| P2 | Company operations | Project, people, decision, and governance facts were scattered across technical screens. | Add a simple role-neutral operating surface over eligible governed local-workspace records without fabricating progress, identity, or employee productivity. | Implemented as a four-tab local preview with exact project-scoped memory/source counts, deduplicated local role records, decision-to-Brain navigation, workspace visibility, audit history, authorization locks, keyboard tab behavior, privacy-scoped projection tests, and responsive Arctic-blue glass UI. |
| P2 | Digital agent UX | Typed automations exposed safe machinery but still read like a technical recipe editor. | Add an assignment-first control room with truthful states, visible capabilities, preview-first actions, and an optional advanced workflow canvas over the same engine. | Implemented and verified. Assigned, Running, Needs attention, Previewed/Waiting, and Completed are derived from real automation/runtime state; external actions remain explicitly locked in the local preview. |
| P2 | Documents UX | Sources were mainly discoverable through search/project cards. | Add a provenance-first Documents ledger with project filtering, recall, and direct graph-node focus. | Implemented. |
| P2 | Navigation | The graph was prominent but not the initial product surface. | Make Brain first/default, retain Home as the attention surface, and connect every domain page back to Brain. | Implemented. |
| P2 | Diagnostics | Sanitized logging existed without a user-auditable support bundle. | Add local diagnostics, integrity status, job health, redacted preview, and explicit export. | Implemented; support bundle excludes memory/source content, project names/paths, credentials, and logs by default. |
| P2 | Website architecture | Legacy versioned CSS/JS layers accumulated. | Consolidate loaded assets, remove proven dead layers, and retain interaction/a11y/performance tests. | Implemented as one maintained site runtime and one guide runtime; obsolete version/hotfix layers were removed. Local interaction, accessibility, layout, focus, production-contract, visual, and strict performance gates are present. Each release candidate must pass the strict performance audit on a clean, idle runner. |
| P2 | Website experience | Public site lacked a coherent opening/product story. | Create a distinctive graph-led opening, provenance story, reduced-motion path, real product proof, and beginner launchpad while preserving the light-blue glass identity. | Implemented as the immediately usable Living Memory Atlas: non-blocking memory ignition, live provenance pulse, central fullscreen graph, role lenses, verified packaged screenshots, natural-flow mobile hero, reduced-motion behavior, and an adaptive beginner guide. Production deployment verification is the final external check. |

## Baseline evidence

At the recorded starting commit plus pre-existing worktree changes:

- `npm run typecheck`: passed.
- `npm test`: 60/60 passed.
- `npm run lint`: passed with one pre-existing unused-expression warning in the graph keyboard handler.
- `npm run electron:compile`: passed after the first security slice.
- Focused graph, IPC, and indexing suites pass after their corresponding changes.

## Business-grade architecture boundary

Enterprise BRACE must be additive:

```text
BRACE Personal data plane (local/offline/private)
                │ explicit publish/sync
                ▼
BRACE Team workspaces (shared, cited, permission-aware)
                │ organization policy
                ▼
BRACE Enterprise control plane (identity, policy, audit, integrations)
```

Personal memory is not an employer surveillance surface. Company administrators may govern company-owned workspaces, sources, retention, and access; they do not silently acquire private local memory.

## Final candidate evidence

Evidence recorded on 2026-09-06 against the integrated candidate:

- `npm run verify`: lint, type checking, 132 unit/integration/security/migration tests, the Next.js 16 production build, Electron compilation, and MCP compilation passed.
- A 5,002-memory stress profile passed every bound: search p50 22.65 ms, p95 29.06 ms, p99 44.31 ms; bounded snapshot read 30.87 ms; privacy-safe export 1.20 s; restart 1.84 ms; RSS growth 195 MB.
- Synthetic Electron E2E passed with all five graph projections, graph fullscreen and keyboard traversal, responsive navigation, Company search/detail and role lenses, exact assistant context preview, the assignment-first digital-agent control room and advanced workflow canvas, connectors, and zero renderer console errors.
- The source executable MCP smoke passed read-only against an isolated temporary database. License review covered 634 installed packages with zero violations; the generated application archive contained 90 files with zero package-content violations; `npm audit --audit-level=high` reported zero vulnerabilities.
- Thirteen current application captures, including the Company brief, were regenerated at 1440×960 from the isolated synthetic workspace and verified against SHA-256 provenance before website synchronization.
- The website production contract, nine interaction journeys, 17 responsive layouts, six keyboard-focus layouts, eight accessibility scenarios, ten visual captures, and performance budgets passed. Median-of-three software-rendered FCP/LCP was 3.28 s desktop, 2.85 s mobile, and 1.95 s for the mobile guide; median maximum long tasks were 467 ms, 448 ms, and 77 ms respectively; CLS was zero throughout.
- Requalification on 2026-09-08 preserved the Arctic glass visual language while replacing expensive full-viewport filter and atlas paint effects. The unchanged strict performance audit against an isolated loopback server passed with median FCP/LCP of 0.97 s desktop, 1.02 s mobile, and 0.44 s for the mobile guide; median maximum long tasks were 171 ms, 682 ms, and 312 ms, with zero CLS, browser errors, or HTTP errors. The production contract, interaction, responsive, focus, accessibility, and visual suites passed again after that change.
- The release privacy test, dependency severity gate, license policy gate, generated package privacy audit, and public-candidate path/secret scan all passed in the integrated qualification set.

## Current release assessment

`BUSINESS-GRADE LOCAL PREVIEW READY`. The repository now includes worker-isolated indexing, staged source commits, recovery, diagnostics, explainable retrieval, 10k-class graph projection/LOD, guarded imports, automation crash recovery, an assignment-first local digital-agent control room, a governed local organization foundation, a privacy-scoped Company brief, verified local identity sessions, explicit shared projections, an encrypted transport-neutral replica log, and a local immutable-plan approval/audit foundation. A stable signed public release remains blocked by Windows code-signing credentials and a proven signed upgrade channel. Networked enterprise collaboration, SSO/SCIM, remote policy enforcement and sync transport, production key management, remote audit checkpoints, external-action agents, and persistent organization agents remain separate product phases and are not implied by the local Company UI.
